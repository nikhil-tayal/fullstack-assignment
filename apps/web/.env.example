# Optional. `next dev` proxies /api to the Nest process on :4001 (see
# next.config.mjs), so local development needs no environment file at all.
# Set this only to point the browser at an API somewhere else.
# NEXT_PUBLIC_API_URL=http://localhost:4001/api
